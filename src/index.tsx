import { render } from "react-dom";
import { App } from "./App";

render(<App />, document.getElementById("root")); //No método render se usam 2 argumentos, primeiro é o elemento a ser renderizado (html) e o segundo é onde ele será inserido.
